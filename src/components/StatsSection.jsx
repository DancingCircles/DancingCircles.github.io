import React from 'react';
import './StatsSection.css';

const StatsSection = () => {
    return (
        <section className="stats-section">
            {/* Content removed to serve as a diagonal transition element */}
        </section>
    );
};

export default StatsSection;
